## 简体中文
- 修复应用内更新公告的显示：之前会把 HTML 源码原样列出、且中英文两段同时显示，现在按界面语言正确分段并渲染。
- 已支持 Linux 版本（AppImage），自动识别 Steam Proton 下的存档路径。
- 安装包体积大幅缩小（约 170MB → 80MB）。

## English
- Fixed the in-app update notes: they previously showed raw HTML with both language sections at once; now they render correctly per UI language.
- Linux build (AppImage) supported, with automatic save-path detection under Steam Proton.
- Much smaller installer (~170MB → 80MB).
